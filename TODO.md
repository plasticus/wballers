# To-Do

The GM's running punch list — playtest feedback and asks not yet built. Superseded `Aug7AfternoonNotes.txt` and `Aug9bugs.md`, which tracked the same kind of thing per-session and got replaced by this single ongoing list once their own outstanding items were folded in here. Finished items don't stay here — they move to `0A_Completed.md` with the real writeup and get deleted from this file, not marked done in place.

## Season Transition

1. **An off-season report** — a direct GM ask (2026-08-12): once a season wraps, a real recap screen/mail item covering: which players declined/improved (the training + season-end aging deltas already computed by `resolveSeasonEndAging`/`runTraining`, just never surfaced as one rollup), who's looking to retire (ties into `retirement_advancer.dart`'s existing eligibility checks), and who's asking to be traded (no such mechanic exists yet -- would need a real trade-request system first, not just the Trade Block preview). Overlaps with the season-transition flow redesign already on the feedback backlog (Dashboard "Begin Draft" entry point, "Start Season N!" splash) -- worth building as one coherent flow rather than two separate features landing at different times. (This ask's 4th facet, roster legality, moved to `roster-legality-notes.md` 2026-08-19 -- its own scoping question, not bundled with these 3 anymore.)

## League & AI Teams

2. **AI teams' real coach quality isn't actually used for training.** Corrected 2026-08-12 -- this item originally (and wrongly) claimed AI teams have no generated coach at all. They do: `AiTeamRoster.coach` is real, generated at league creation via `generateCoach` (`league_generator.dart`), and `coachesByAbbreviation()` already exposes it for real in-game coaching bonuses. The actual, narrower gap: `resolveAiTeamSeasonTraining`/`resolveAiTeamSeasonEndAging` (`training_advancer.dart`) hardcode a flat `CoachStats.neutral` (development 50) instead of reading `aiTeam.coach.stats.development` -- every AI team's real, already-varied coach quality is used correctly for match-day bonuses but ignored for training/aging. Fix is small: read the real coach's development rating in both functions instead of the neutral fallback. Still relevant to the growth-curve levers shipped this session -- every AI-drafted prospect currently trains under the same effective coach quality regardless of who's actually coaching her, capping how many AI-team prospects can realistically reach the 90s tier. (Coach of the Year/Executive of the Year, blocked on "AI teams have no coach" in the Awards catalog brainstorm, `0A_Completed.md` -- also worth re-checking against this correction; they may already be unblocked.)
3. **Team identities** — a direct GM ask (2026-08-12), explicitly deferred: "at some point, I might need team identities -- like what type of coach/players they'll go after." No detail yet on shape (a personality/archetype per AI team driving free-agency and draft preferences, roster construction philosophy, etc.) -- needs a real design pass before building, not just a quick add. Likely depends on item 2 above (a team identity expressed partly through what kind of coach it hires) landing first.

## Pre-Game Screen

4. **Wire real coach-matchup data / a richer Analyst pick model.** A follow-up to the "Matchup Analysis" screen shipped 2026-08-12 (`0A_Completed.md`) -- the 5 analysts currently each read one real number (team offense/defense/physical/overall, or best single player) via `matchup_analysis.dart`'s `analystVerdicts`. `coachesByAbbreviation` (real, already used for in-game coaching bonuses) isn't factored into any pick yet -- worth revisiting once there's a natural "coach quality" criterion to add, rather than bolting one on arbitrarily.
5. **Retired-veteran seat only checks "still on `roster`."** The Analyst panel's seat 1 swaps from the generic "Preston" look to the real narrative veteran's own portrait once she's no longer found in `Franchise.roster` -- today the *only* way that happens is retirement, so this is reliable, but it would also (incorrectly) trigger the day a trade/waive mechanic for the GM's own roster ships, since neither exists yet. Revisit this check once either does.

## In-Game Strategy

6. **Wire real coach-quality data into the offense/defense bonuses.** A follow-up to Offense Shape/Defensive Tactic shipped 2026-08-14 (`0A_Completed.md`) -- `offenseBonusFor`/`defenseBonusFor` (`matchup/domain/offense_shape.dart`/`defensive_tactic.dart`) are flat per-shape/per-tactic constants today, with no coach-skill input at all. Worth revisiting once there's a natural way to let a strong Offense/Defense coach amplify or dampen these (mirrors the existing, separate `coachMatchupBonus` mechanism, `possession_engine.dart`) rather than bolting one on arbitrarily.
7. **Show the chosen Defensive Tactic (and both teams' Offense Shape) on the post-game recap.** Not built -- the GM picks a tactic pre-game and it's applied for real, but nothing on `GameResultScreen` afterward says which one was used or what shape either team ran, so there's no feedback loop for "did that actually work?" A natural, small follow-up once there's appetite for it.
8. **Live game visual + quarter-break coaching options.** A direct GM ask (2026-08-17) -- scoping conversation happened, but building is blocked on the stamina/fatigue system (`0B_Planned.md`'s Phase 3 stamina appendix, now promoted to a blocker), so it comes second. Decisions locked in during the scoping pass, to pick back up once stamina/fatigue is real:
   - **Breaks**: end of Q1, Q2 (halftime), Q3, plus one more in Q4 if the game is within **7 points** (the recorded design-doc number, not the 9 floated mid-conversation) at the 2:00 mark.
   - **Visual**: an abstracted court diagram (dots/icons for the 10 players, animated shot/pass indicators) -- a deliberate override of `0B_Planned.md`'s Phase 3-before-Phase-4 sequencing (text feed first, court visual later); the GM chose to go straight to the court diagram instead.
   - **Scope**: the GM's own scheduled game only, same as the existing Matchup Preview/Game Result pattern -- every other league game keeps instant bulk-simulating with AI always on Balanced, no equivalent AI-side adjustment logic.
   - **Opt-out**: add a Watch Live vs. Sim Instantly toggle rather than forcing every game through the live flow.
   - **Coaching-option catalog: locked (2026-08-17).** 10 options, full writeup in `0B_Planned.md`'s quarter-break bullet -- this is the short version. Every option nets more pro than con (explicitly standing in for a morale mechanic that doesn't exist), lasts one quarter or the final ~2 minutes depending on which stoppage offered it, and the 4 pace-related options (Full-Court Press/Park the Bus/Pace Yourself/Pick Up the Pace) reuse the engine's existing blowout-pace possession-duration lever rather than inventing a new one.
   - **Selection logic: also locked (2026-08-17).** Mostly a random draw of 3, not a fuller situational picker (a direct GM call) -- 2 eligibility gates on top of the randomness: Stop the Bleeding is guaranteed a slot when the opponent's mid an 8+ unanswered run, and Park the Bus only enters the pool at a 2nd-half break (Q3-end or the Q4 2:00-mark stoppage). Everything else is an equal-odds random draw for whatever slots remain.
   - **Mechanical system: built (2026-08-17).** `lib/features/matchup/domain/coaching_option.dart` -- the full 10-option catalog, `coachingBonusFor` (the continuous per-possession offense/defense/rebounding/disruption/pace/stamina-multiplier effects), `applyFireTheTeamUp`/`pickPlayerToRest` (the 2 instant, one-time actions), and `offerCoachingOptions` (the locked selection logic, eligibility gates and all). Wired for real into `possession_engine.dart` (`offenseCoachingBonus`/`defenseCoachingBonus` params, threaded through every rating contest plus a new coach-picked pace knob on `_rollActionSeconds`) and `match_engine.dart` (`homeCoachingPicker`/`awayCoachingPicker`, both optional and `null` by default -- same "AI always Balanced" opt-in posture `homeDefenseTactic`/`awayDefenseTactic` already established, so no existing caller's behavior changes unless it opts in; fires at all 3 ordinary quarter breaks plus the Q4 late-game stoppage; tracks each side's real unanswered-run for Stop the Bleeding's trigger). `substitution_policy.dart`'s `pickOnCourt`/`substituteForFoulOut` both gained a `rested` param for Rest a Player's pick. Covered by `coaching_option_test.dart` (the domain layer in isolation) and new cases in `match_engine_test.dart` (the wiring itself -- determinism holds with pickers attached, a picker gets called at every break with a real 3-option menu, Fire the Team Up actually moves energy). The Live Game Lab's coaching-break sheet now shows this real catalog/draw instead of the old DefensiveTactic-re-pick placeholder. **Deliberately synchronous, for now** -- see the next bullet for why, and what that still leaves undone.
   - **Architecture: underway (2026-08-18), staged.** `simulateMatch` (`match_engine.dart`) used to run a game as one uninterruptible synchronous loop; `advanceGameDay`/`season_advancer.dart` still batch-simulates every scheduled game for the day (GM's included) before sorting out which result belongs to the GM -- that second half is still true. Building this in stages rather than one large, risky change to code every AI-vs-AI game in the season also runs through:
     - **Stage 1: done.** The whole quarter-by-quarter loop was pulled out of `simulateMatch` into a new internal `_GameSimulation` class (`prepareQuarter`/`runPossessions`/`wrapUpQuarter`). `simulateMatch` itself is now a thin wrapper -- exact same public signature and behavior, verified via the full test suite (all 1012 tests, including every determinism case and every season-simulation test that exercises it transitively) plus `tool/fatigue_diagnostic.dart` producing identical numbers before and after.
     - **Stage 2: done.** A new, genuinely `async` sibling, `simulateMatchLive`, drives the same `_GameSimulation` one quarter (or Q4 late-break segment) at a time: computes a segment instantly (nothing here is gated on real wall-clock time), hands its events to a caller-supplied `onSegmentComplete` callback and awaits it, then (if a break is due) awaits a real `LiveCoachingPicker` (`coaching_option.dart` -- `CoachingOptionPicker`'s async twin) before continuing. `simulateMatch` and every one of its ~35 existing callers are completely untouched -- `simulateMatchLive` is an entirely separate entry point built on shared internals (`_processOnePossession`, `_applyCoachingPick`, `_resolveBreakSync`/`_resolveBreakLive`). Covered by new `match_engine_test.dart` cases: same result shape as `simulateMatch` for equivalent inputs, a live picker is genuinely awaited and its pick takes effect, determinism holds with live pickers attached, and -- the one that actually matters most -- concatenating every segment `onSegmentComplete` receives reproduces the final event log exactly (nothing missed, nothing duplicated).
     - **Stage 3: done.** `lib/features/match/presentation/live_beat_translator.dart` -- a new `LiveBeatTranslator` turning `simulateMatchLive`'s real `MatchEvent`s into `LiveBeat`s (the public, real-data analog of the Lab's private `_LabBeat`), resolved against `play_by_play_phrases.toml`'s wording bank same as the Lab does. Covers every `MatchEventType` this engine actually produces, including 3 that had no matching category before now (`offensive_rebound`/`shot_clock_violation`/`shooting_foul_no_make`, added to the TOML -- 141 phrases, 22 categories). Real, not invented, positional variety: the first pass of a possession (a genuine structural fact `_GameSimulation.possessions`'s grouping gives for free, not guessed at) reads as bringing the ball up; later ones read as generic ball movement -- the same "PG setting up the offense" distinction the GM asked for, done with real data instead of a hand-placed filler beat. And-one detection, steal/block credit-swapping, and player2-naming (assist, steal, shooting-foul-no-make) all mirror the Lab's own conventions. Court-zone/chip-index placement is still a documented heuristic, not real geometry -- `MatchEvent` carries no position data at all. Required 2 small `simulateMatchLive` refinements to give the translator what it needed: `_GameSimulation.possessions` (grouped events, not just the flat list) and a real `MatchEvent(type: tipOff, ...)` (the enum case existed but nothing ever emitted one). Covered by `live_beat_translator_test.dart`: 10 full real games translate with zero unresolved `{player}`-style placeholders in any beat (the exact bug class caught live in the Lab once already), score deltas summed across every beat match the real final score exactly, and-one detection actually fires in real games, and a real `isClutch` bug (misread as clutch before any possession had even run) was caught and fixed by the test itself before this ever reached a screen. Full suite (1025 tests) and `flutter analyze` clean.
     - **Stage 4: done (2026-08-18, in-place replacement -- a direct GM call: "yep, in-place replacement plz" over building a parallel screen).** `live_game_lab_screen.dart`'s guts now drive a real `simulateMatchLive` game instead of the old 27-beat hand-scripted list; essentially all of its widget/rendering code (the court painter, blip animations, score bug, coaching-break sheet) survived unchanged, aliased onto the real public types (`LiveTeam`/`LiveZone`/`LiveHighlight`/`LiveBeat`) via private typedefs (`_Team = LiveTeam`, etc.) so none of it needed touching. What actually changed is `_LiveGameLabScreenState`: a real 12-player roster per side (`generateAiRoster`, generated once in `initState`, replayable), a real `LiveBeatTranslator` turning each `LiveGameSegment` into beats, and a `Completer`-based `_waitForAdvance` that awaits between beats so `simulateMatchLive` (which computes a whole segment instantly) only proceeds once the GM has actually seen the current one at their chosen Slow/Med/Fast/Step pace -- Play/Pause/Step all resolve or leave that completer pending rather than driving a `Timer.periodic` over a fixed list. The real `LiveCoachingPicker` shows the same `_CoachingBreakSheet` the Lab always had, now fed the engine's own already-drawn `CoachingBreakContext.offered` menu instead of re-rolling one internally. Verified live on the Android emulator: real player names/positions/jersey numbers throughout, real score/clock/quarter tracking every beat, a real Q2 coaching break firing at the real score with the real 3-option menu, a real pick taking effect and the game resuming correctly into Q2. `flutter analyze` and the full suite (1025 tests) still clean -- no widget test existed or was added for this screen, consistent with it being a manual/live-verified dev screen throughout this whole effort.
     - **Stage 5: done (2026-08-18).** `MatchPreviewScreen` gets a real Watch Live toggle (`app_preferences.dart`'s `watchLiveProvider` -- remembers the GM's last pick, a direct ask, rather than resetting every visit) next to Play Game. On: pushes `LiveGameLabScreen` with the real franchise/game/tactic; that screen drives `simulateMatchLive` itself, then folds the result into the rest of the day via a new `advanceGameDayWithOwnResult` (`current_franchise_provider.dart`) and hands off to `GameResultScreen`. Off: unchanged Sim Instantly path. `season_advancer.dart`'s `advanceToNextGameDay` gained an `ownGameAlreadyPlayed` param for this -- slots the GM's own already-played `MatchResult` into that day's batch instead of re-simulating it, while every other game that day still bulk-simulates normally from the same continuing `random` stream (an accepted, one-time side effect: choosing Watch Live shifts what every other team's game that day draws, since the GM's own game no longer consumes from the shared stream at its natural position -- doesn't affect either path's own reproducibility). No new save-schema state for "a live game is in progress," per the locked design call -- nothing persists until the live game actually finishes.
       - **Verified live** (2026-08-18): a real Wichita Witches vs. Tijuana Frontera preseason game, watched start to finish via Watch Live -- real title/colors/rosters, a real Q2 coaching break at the real score with the real 3-option menu, a real pick resuming correctly into Q2. Also caught and fixed a real `Timer` leak (`_advanceTimer` was never stored/cancelled) via a widget test that failed on a stray pending timer -- `dispose()` now cancels it.
       - **Post-launch live-watch fixes** (2026-08-18, from actually watching real games play out): `LiveBeatTranslator._chipIndexFor` was hashing off the player's own id, so the same player always landed in the same spot on the floor every single time -- now genuinely random per beat. A made basket never actually triggered the (already-existing but unwired) `LiveBeat.isInbound` flag, so the very next possession's first pass looked like the scoring team still had the ball -- wired up for real, including 2 real edge cases (an and-one's own make doesn't guarantee the *trip* ended in a make if the final free throw misses and gets live-rebounded; the flag must not carry across a quarter break, since `match_engine.dart` resets who starts each quarter independently of how the previous one ended). Fast speed now skips every beat that isn't a shot attempt (or a clutch free throw) entirely, and quadruples the on-screen time for the one it does show (the +2/+3/miss-X result was getting cut off almost instantly at Fast's normal pace). "Coach Picker Lab" moved off the Training screen onto Settings' Developer section, next to Live Game Lab. Training Report cards now call out a whole-point OVR change ("OVR 67 -> 68") separately from the field-delta chips.
   - **Live-integration decisions locked (2026-08-18), before writing any of this code**:
     - **Day-advance flow**: on a day with the GM's own scheduled game, advancing the day opens the live view first, as its own screen -- the other ~19 games' worth of daily matchups keep bulk-simulating automatically once it ends, same ordering as today, just with the GM's game pulled out and shown live first rather than folded into the batch.
     - **Watch Live vs. Sim Instantly**: a per-game toggle on the Matchup Preview screen, next to Play Game -- not a global Settings default. The GM can skip a live view on a given day without changing anything for next time.
     - **Interruption**: no resume support. If the app is backgrounded or closed mid-live-game, it just finishes bulk-simulated (identical to picking Sim Instantly) -- no new save-schema state for an in-progress game, consistent with the standing pre-release call to avoid save-schema churn.
     - **Event-to-beat translation**: every real `MatchEvent` becomes its own beat -- no filtering down to "only notable events." A real quarter's full granularity (pass attempts, disruptions, redirects, not just shots/turnovers/fouls) surfaces in the feed; Fast/Step modes (already built, `live_game_lab_screen.dart`) are how the GM compresses a 60-80+-beat quarter into something watchable, rather than the app deciding what's worth showing. Free throws stay hidden-unless-clutch either way (already-built lab behavior, unaffected by this).
   - **Blocker resolved and built (2026-08-17): stamina/fatigue system implemented, tuned, wired into live gameplay, and validated.** Real code now (`lib/features/match/engine/fatigue.dart`, wired into `match_engine.dart`, `MatchResult.finalEnergy`) -- full writeup in `0B_Planned.md`'s stamina appendix, this is the short version. Formulas were hand-checked against `substitution_policy.dart`'s target-minutes table first, then validated with a real diagnostic (`tool/fatigue_diagnostic.dart`, run via `flutter test tool/fatigue_diagnostic.dart`) across 20 real games with real generated rosters -- twice: once to retune the drain/recovery rates (the initial rates left even the heaviest-used rotation tier under a 2% average penalty, too mild for a direct GM ask that ordinary heavy-minute play should feel it), and again after wiring the resulting penalty into every `possession_engine.dart` rating contest a fatigued player touches (passing, shooting, free throws, blocking, rebounding, both ends of the floor -- `_fatigueBonus`, the same accumulator slot `OffenseShapeBonus`/`DefenseTacticBonus`/`coachMatchupBonus` share) to confirm the existing scoring calibration (~201 combined avg, `match_engine.dart`'s own doc comment) survived: it did, 205.1 avg across the 20-game post-wiring run, comfortably within normal noise. Final numbers: top two rotation tiers (30/26-minute targets) take a 6-9% average penalty on nearly all their player-games, with a small tail into severe (<50 energy) territory; everything at 14 minutes and under stays essentially untouched. `MatchResult.finalEnergy` is kept on the result on purpose, past what gameplay itself needs, for a later post-game consumer -- e.g. a "most tired players" callout to help the GM decide who to move down the rotation (a direct GM ask, 2026-08-17); no such consumer exists yet, this just keeps the door open without inventing save-schema persistence nobody's asked for (same transient, game-scoped lifetime as the box score). **Still open**: substitutions still don't react to fatigue -- `pickOnCourt`/`targetMinutesFor` are driven purely by target minutes and foul-outs, no energy-aware bench logic yet. That, plus a real UI consumer of `finalEnergy`, are natural follow-ups once there's appetite -- and the quarter-break coaching-options catalog (this item's other half, above) now has real stamina-related levers to draw on instead of being blocked on this system existing.

## Troubleshooting

9. **Game log / box score history.** Moved here 2026-08-19 (from `8-18pm-notes.md`, since deleted) -- not built. The GM wants some way to look back at past games' box scores for troubleshooting (a stat looking off, a result that doesn't add up) without it being tied to any one screen's current-game context. `PlayedGameDetailScreen` (`results_screen.dart`) already renders a full past game's box score and is reachable from Results/Schedule/the League tab's Cup rounds -- whatever this ends up being is more likely a better *entry point* into games this GM's own team played (a real searchable/filterable log) than a new rendering of the data itself, which mostly already exists.
