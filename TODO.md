# To-Do

The GM's running punch list — playtest feedback and asks not yet built. Superseded `Aug7AfternoonNotes.txt` and `Aug9bugs.md`, which tracked the same kind of thing per-session and got replaced by this single ongoing list once their own outstanding items were folded in here. Finished items don't stay here — they move to `0A_Completed.md` with the real writeup and get deleted from this file, not marked done in place.

## Coaching

1. **Wire up the coach stats that still do nothing** — confirmed 2026-08-10: only Development affects anything real (`training_advancer.dart`'s growth multiplier). Offense/Defense are the buildable half: a real match-simulation modifier off the GM's own coach's stats, the same pattern `kHomeAdvantageBonus` already uses for home-court advantage (`possession_engine.dart`) -- a flat rating bump/penalty for the team, not a full Phase 3 tactics system. Motivation and Management are a bigger lift and stay blocked: they're tied to systems that don't exist anywhere in the codebase yet (morale/chemistry, trades), so those two ride along with whichever system lands first, same as the original note said. **In progress 2026-08-11**: Offense/Defense now resolve as a real coach-vs-coach matchup bonus in the match engine.

## Match Preview

2. **A coach-matchup flavor line on `MatchPreviewScreen`** — a direct GM ask (2026-08-11): a single compound sentence putting the two coaches' Offense/Defense difference into words before the GM fires up the game, e.g. "Coach A of the Ding-Dongs brings a slight advantage with her offensive schemes, while Coach B of the Dingleberries gets a vastly superior defensive effort out of her players." The GM recalled this alongside "something about 5 commentators predicting the game" -- a search of the codebase and every planning doc turned up no existing multi-commentator prediction feature, so that part may be from an earlier, uncaptured conversation; worth confirming with the GM before assuming it needs building from scratch too. Natural to build once the coach Offense/Defense bonus above lands for real, since the flavor text and the mechanical bonus should read the same way (a real Offense/Defense point gap, not an invented one).
